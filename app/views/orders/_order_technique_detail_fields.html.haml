.nested-fields.form-inline
  .form-group
    -# = f.label :order_option_id, "🐤加工方法"
    = f.collection_select(:technique_id, Technique.all, :id, :technique_name)
    = f.hidden_field :progress_id, value: 1
    = f.hidden_field :representative_user_id, value: current_user.id
    = f.hidden_field :pasteup_user_id, value: 1
    = f.hidden_field :maker_id, value: 1
    %br/
    = link_to_remove_association "🐤加工方法を削除❌", f