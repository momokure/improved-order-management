.nested-fields.form-inline
  %br/
  .form-group
  = f.label :shipment_note, "発送備考欄"
  = f.text_area :shipment_note
  = f.hidden_field :user_id, value: current_user.id
  %br/
  %br/
  = link_to_remove_association "発送備考を削除❌", f