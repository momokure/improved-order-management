.nested-fields.form-inline
  %br/
  .form-group
  = f.label :order_tag_note, "タグの受取備考欄"
  = f.text_area :order_tag_note
  = f.hidden_field :user_id, value: current_user.id
  %br/
  %br/
  = link_to_remove_association "タグの受取備考を削除❌", f